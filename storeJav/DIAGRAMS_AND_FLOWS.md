# 📊 Visual Diagrams & Flow Charts

## 1. Request Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Browser                            │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ↓
                    GET /callback?code=ABC
                             │
                             ↓
         ┌───────────────────────────────────────────┐
         │         AuthController                    │
         │  ┌─────────────────────────────────────┐  │
         │  │ handleCallback(code, state)         │  │
         │  │                                     │  │
         │  │ 1. Log request                      │  │
         │  │ 2. Call oauthService.exchangeCode()│  │
         │  │ 3. Call bankingService.getAccounts()│ │
         │  │ 4. Return response                  │  │
         │  └─────────────────────────────────────┘  │
         └────────────┬────────────────────┬─────────┘
                      │                    │
          ┌───────────▼──────────┐  ┌──────▼─────────────────┐
          │ OAuthService         │  │ BankingApiService      │
          ├──────────────────────┤  ├────────────────────────┤
          │ exchangeCode()       │  │ getAccounts()          │
          │ • Build request      │  │ • Build headers        │
          │ • POST to token URI  │  │ • GET account data     │
          │ • Parse response     │  │ • Return JSON          │
          │ • Handle exceptions  │  │ • Handle exceptions    │
          └──────────┬───────────┘  └──────────┬─────────────┘
                     │                         │
                     ↓                         ↓
         ┌───────────────────────────────────────────┐
         │      Spring RestTemplate                  │
         │  HTTP Client for all API calls            │
         └────────────────────┬────────────────────┘
                              │
                ┌─────────────┴──────────────┐
                ↓                            ↓
      OAuth Token API          Banking Accounts API
      https://api.sparebank... https://api.sparebank...
                │                            │
                ↓ (token)                    ↓ (accounts)
      ┌─────────────────────────────────────────┐
      │      GlobalExceptionHandler             │
      │  • Catches exceptions from services     │
      │  • Returns JSON error response          │
      │  • Sets proper HTTP status codes        │
      └────────────────────┬────────────────────┘
                           │
                           ↓
                    200 OK / 401 / 502
                           │
                           ↓
         ┌─────────────────────────────────────┐
         │         User Browser                │
         │  Display account data or error      │
         └─────────────────────────────────────┘
```

---

## 2. Service Layer Architecture

```
                    ┌──────────────────────┐
                    │   AuthController     │
                    │   (Orchestrator)     │
                    └──────┬───────┬───────┘
                           │       │
                ┌──────────┘       └──────────┐
                │                             │
                ↓                             ↓
         ┌────────────────┐          ┌─────────────────────┐
         │ OAuthService   │          │ BankingApiService   │
         ├────────────────┤          ├─────────────────────┤
         │ + exchangeCode │          │ + getAccounts       │
         │ - restTemplate │          │ - restTemplate      │
         │ - oauthConfig  │          │                     │
         └────────┬───────┘          └──────────┬──────────┘
                  │                             │
                  ↓                             ↓
         ┌─────────────────────────────────────────┐
         │  GlobalExceptionHandler                 │
         │  ├─ OAuthException → 401               │
         │  ├─ BankingApiException → 502          │
         │  └─ Exception → 500                    │
         └──────────────────┬──────────────────────┘
                            │
                            ↓
                      RestTemplate
                      (HTTP Client)
```

---

## 3. Exception Handling Flow

```
┌────────────────────────────────────────────────────────────┐
│                    Service Layer                           │
│                                                            │
│  OAuthService / BankingApiService                         │
│       │                                                   │
│       └─ Calls RestTemplate                              │
│           │                                              │
│           ├─ Success → Return data                       │
│           │                                              │
│           └─ Failure → Throw exception                   │
│               │                                          │
│               ├─ RestClientException                    │
│               │   → Wrap in OAuthException              │
│               │                                          │
│               └─ JsonProcessingException                │
│                   → Wrap in OAuthException              │
└──────────────────┬───────────────────────────────────────┘
                   │
                   ↓
         ┌──────────────────────────────┐
         │ Controller catches exception  │
         │ Re-throws to framework        │
         └──────────────────┬────────────┘
                            │
                            ↓
         ┌──────────────────────────────────┐
         │ GlobalExceptionHandler           │
         │ @ControllerAdvice               │
         │                                 │
         │ @ExceptionHandler(               │
         │   OAuthException.class          │
         │ )                               │
         │ └─ Returns 401 Unauthorized     │
         │                                 │
         │ @ExceptionHandler(               │
         │   BankingApiException.class     │
         │ )                               │
         │ └─ Returns 502 Bad Gateway      │
         │                                 │
         │ @ExceptionHandler(               │
         │   Exception.class               │
         │ )                               │
         │ └─ Returns 500 Internal Error   │
         └──────────────────┬──────────────┘
                            │
                            ↓
         ┌──────────────────────────────┐
         │ JSON Error Response           │
         │ {                             │
         │   "error": "...",             │
         │   "message": "...",           │
         │   "details": "..."            │
         │ }                             │
         └──────────────────────────────┘
```

---

## 4. Dependency Injection Graph

```
┌─────────────────────────────────────────────────┐
│         Spring Application Context              │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ RestTemplateConfig (@Configuration)      │  │
│  │ ├─ @Bean                                 │  │
│  │ └─ RestTemplate                          │  │
│  └──────────────┬──────────────────────────┘  │
│                 │                              │
│  ┌──────────────▼──────────────────────────┐  │
│  │ SparebankOauthConfig (@Component)        │  │
│  │ @ConfigurationProperties(prefix=...)    │  │
│  │ ├─ clientId                             │  │
│  │ ├─ clientSecret                         │  │
│  │ ├─ redirectUri                          │  │
│  │ └─ tokenUri                             │  │
│  └──────────────┬──────────────────────────┘  │
│                 │                              │
│  ┌──────────────▼──────────────────────────┐  │
│  │ OAuthService (@Service)                 │  │
│  │ @Autowired RestTemplate                 │  │
│  │ @Autowired SparebankOauthConfig         │  │
│  └──────────────┬──────────────────────────┘  │
│                 │                              │
│  ┌──────────────▼──────────────────────────┐  │
│  │ BankingApiService (@Service)            │  │
│  │ @Autowired RestTemplate                 │  │
│  └──────────────┬──────────────────────────┘  │
│                 │                              │
│  ┌──────────────▼──────────────────────────┐  │
│  │ AuthController (@Controller)            │  │
│  │ @Autowired OAuthService                 │  │
│  │ @Autowired BankingApiService            │  │
│  └──────────────┬──────────────────────────┘  │
│                 │                              │
│  ┌──────────────▼──────────────────────────┐  │
│  │ GlobalExceptionHandler (@ControllerAdvice) │
│  │ (No dependencies needed)                │  │
│  └──────────────────────────────────────────┘  │
└─────────────────────────────────────────────────┘
```

---

## 5. Data Flow

```
┌──────────────────────────────────────────────────────────┐
│           OAuth Callback Request                         │
│   GET /callback?code=AUTH123&state=STATE456             │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ↓
    ┌────────────────────────────────┐
    │ AuthController.handleCallback()│
    │ - code: "AUTH123"              │
    │ - state: "STATE456"            │
    └────────────────┬───────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ↓                         ↓
    OAuthService           BankingApiService
    - Prepare request       (waiting for token)
    - POST /oauth/token
    - Headers:
      Content-Type: 
      application/x-www-
      form-urlencoded
    - Body:
      client_id: ****
      client_secret: ****
      code: AUTH123
      grant_type: authorization_code
      state: STATE456
      redirect_uri: http://...
        │
        ↓
    ┌─────────────────────────┐
    │ TokenResponse           │
    │ {                       │
    │  "access_token": "abc"  │
    │  "token_type": "Bearer" │
    │  "expires_in": 3600     │
    │  "refresh_token": "..."│
    │ }                       │
    └──────────┬──────────────┘
               │
               ↓ (extract access_token)
          ┌──────────────────────┐
          │ BankingApiService    │
          │ - Prepare request    │
          │ - GET /accounts      │
          │ - Headers:           │
          │   Authorization:     │
          │   Bearer abc         │
          │   Accept:            │
          │   application/...    │
          └──────────┬───────────┘
                     │
                     ↓
          ┌──────────────────────┐
          │ Accounts Response    │
          │ {                    │
          │  "accounts": [{...}] │
          │ }                    │
          └──────────┬───────────┘
                     │
                     ↓
        ┌────────────────────────┐
        │ Return to Controller    │
        │ ResponseEntity.ok()    │
        │ (200 OK + body)        │
        └────────────┬───────────┘
                     │
                     ↓
        ┌────────────────────────┐
        │ Response to Browser    │
        │ 200 OK                 │
        │ {                      │
        │  "accounts": [{...}]  │
        │ }                      │
        └────────────────────────┘
```

---

## 6. Configuration Flow

```
┌─────────────────────────────────────┐
│   Environment Variables             │
│   SPAREBANK_CLIENT_ID = "..."      │
│   SPAREBANK_CLIENT_SECRET = "..."  │
└──────────────┬──────────────────────┘
               │
               ↓
┌─────────────────────────────────────┐
│   application.yml                   │
│   sparebank:                        │
│     oauth:                          │
│       client-id:                    │
│         ${SPAREBANK_CLIENT_ID}     │
│       client-secret:                │
│         ${SPAREBANK_CLIENT_SECRET} │
│       redirect-uri: http://...     │
│       token-uri: https://...       │
└──────────────┬──────────────────────┘
               │
               ↓
┌─────────────────────────────────────┐
│   SparebankOauthConfig              │
│   @ConfigurationProperties          │
│   (prefix = "sparebank.oauth")      │
│                                     │
│   - clientId                        │
│   - clientSecret                    │
│   - redirectUri                     │
│   - tokenUri                        │
└──────────────┬──────────────────────┘
               │
               ↓
┌─────────────────────────────────────┐
│   OAuthService                      │
│   @Autowired                        │
│   SparebankOauthConfig config       │
│                                     │
│   Uses:                             │
│   - config.getClientId()           │
│   - config.getClientSecret()       │
│   - config.getRedirectUri()        │
│   - config.getTokenUri()           │
└─────────────────────────────────────┘
```

---

## 7. Before vs After Comparison

```
BEFORE                                  AFTER
───────────────────────────────────────────────────────────

ProcessBuilder                          RestTemplate
├─ Hardcoded credentials    ──────→    ├─ Injected config
├─ External curl dependency ──────→    ├─ Spring built-in
├─ Manual JSON parsing      ──────→    ├─ Jackson automatic
└─ Poor error handling      ──────→    └─ Proper exceptions

Monolithic Controller                   Layered Architecture
├─ Mixed concerns           ──────→    ├─ Service layer
├─ Hard to test            ──────→    ├─ Easy to mock
├─ Complex logic           ──────→    ├─ Simple orchestration
└─ Poor maintainability    ──────→    └─ Clean code

Basic Error Handling                    Centralized Handling
├─ Try-catch blocks        ──────→    ├─ @ControllerAdvice
├─ Inconsistent responses  ──────→    ├─ JSON format
├─ Logged to console       ──────→    └─ SLF4J logging
└─ No type specificity

No Tests                                Comprehensive Tests
├─ No coverage             ──────→    ├─ Unit tests
├─ Hard to verify          ──────→    ├─ Integration tests
└─ Risky changes           ──────→    └─ Safe refactoring
```

---

**All diagrams show the improved architecture post-refactoring** ✅

